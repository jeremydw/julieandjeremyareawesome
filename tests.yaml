tests:

  - route: home
    assertInHtml:
      - About us
      - Travel &amp; Schedule
      - Our wedding is on
  
  - route: redirect_guest
    assertInHtml:
      - window.location = '/rsvp/';
  
  - route: redirect_weddingparty
    assertInHtml:
      - window.location = '/rsvp/';
